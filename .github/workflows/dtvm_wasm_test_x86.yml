name: DTVM-WASM test CI in x86-64

on:
  push:
    paths-ignore:
      - 'docs/**'
      - 'resources/**'
      - '*.md'
      - '.gitignore'
  pull_request:
    paths-ignore:
      - 'docs/**'
      - 'resources/**'
      - '*.md'
      - '.gitignore'
permissions:
    contents: read

# Shared FetchContent cache root for all container jobs. The hook in
# CMakeLists.txt (commit 96707a2 lines 8-18) picks this up as the base
# dir for FetchContent populations. The cache key includes the generator
# namespace because CMake writes generator-specific FetchContent subbuilds.
env:
  FETCHCONTENT_BASE_DIR: /github/home/.fetchcontent
  SCCACHE_GHA_ENABLED: "true"
  SCCACHE_GHA_VERSION: dtvm-ci-sccache-v2-gcc11-llvm15-ninja
  SCCACHE_BASEDIRS: ${{ github.workspace }}
  SCCACHE_IDLE_TIMEOUT: "0"
  DTVM_CI_USE_NINJA: "1"
  DTVM_CI_USE_SCCACHE: "1"

jobs:
  build_test_interp_on_x86:
    name: Build and test DTVM interpreter on x86-64
    runs-on: ubuntu-latest
    container:
      image: dtvmdev1/dtvm-dev-x64:main
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          submodules: "true"
      - name: Cache FetchContent deps
        uses: actions/cache@v4
        with:
          path: /github/home/.fetchcontent
          key: ${{ runner.os }}-fc-${{ github.workflow }}-ninja-v2-${{ hashFiles('third_party/AddDeps.cmake') }}
      - name: Code Format Check
        run: |
          ./tools/format.sh check
      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - name: Test Git clone
        run: |
          git clone https://github.com/asmjit/asmjit.git
      - name: Install llvm
        run: |
          echo "current home is $HOME"
          export CUR_PROJECT=$(pwd)
          cd /opt
          # ./install_llvm15.sh
          # ./install_rust.sh
          cd $CUR_PROJECT
          export LLVM_SYS_150_PREFIX=/opt/llvm15
          export LLVM_DIR=$LLVM_SYS_150_PREFIX/lib/cmake/llvm
          export PATH=$LLVM_SYS_150_PREFIX/bin:$PATH
          cd tests/wast/spec
          git apply ../spec.patch
          cd $CUR_PROJECT
          export CMAKE_BUILD_TARGET=Debug
          export ENABLE_ASAN=true
          export RUN_MODE=interpreter
          export INPUT_FORMAT=wasm
          export ENABLE_LAZY=true
          export ENABLE_MULTITHREAD=true
          export TestSuite=microsuite
          export CPU_EXCEPTION_TYPE='check'
          export ENABLE_GAS_METER=false

          bash .ci/run_test_suite.sh

  build_test_singlepass_on_x86:
    name: Build and test DTVM singlepass on x86-64
    runs-on: ubuntu-latest
    container:
      image: dtvmdev1/dtvm-dev-x64:main
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          submodules: "true"
      - name: Cache FetchContent deps
        uses: actions/cache@v4
        with:
          path: /github/home/.fetchcontent
          key: ${{ runner.os }}-fc-${{ github.workflow }}-ninja-v2-${{ hashFiles('third_party/AddDeps.cmake') }}
      - name: Code Format Check
        run: |
          ./tools/format.sh check
      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - name: Test Git clone
        run: |
          git clone https://github.com/asmjit/asmjit.git
      - name: Install llvm
        run: |
          echo "current home is $HOME"
          export CUR_PROJECT=$(pwd)
          cd /opt
          # ./install_llvm15.sh
          # ./install_rust.sh
          cd $CUR_PROJECT
          export LLVM_SYS_150_PREFIX=/opt/llvm15
          export LLVM_DIR=$LLVM_SYS_150_PREFIX/lib/cmake/llvm
          export PATH=$LLVM_SYS_150_PREFIX/bin:$PATH
          cd tests/wast/spec
          git apply ../spec.patch
          cd $CUR_PROJECT
          export CMAKE_BUILD_TARGET=Debug
          export ENABLE_ASAN=true
          export RUN_MODE=singlepass
          export INPUT_FORMAT=wasm
          export ENABLE_LAZY=true
          export ENABLE_MULTITHREAD=true
          export TestSuite=microsuite
          export CPU_EXCEPTION_TYPE='check'
          export ENABLE_GAS_METER=false

          bash .ci/run_test_suite.sh

  build_test_multipass_on_x86:
    name: Build and test DTVM multipass on x86-64
    runs-on: ubuntu-latest
    container:
      image: dtvmdev1/dtvm-dev-x64:main
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          submodules: "true"
      - name: Cache FetchContent deps
        uses: actions/cache@v4
        with:
          path: /github/home/.fetchcontent
          key: ${{ runner.os }}-fc-${{ github.workflow }}-ninja-v2-${{ hashFiles('third_party/AddDeps.cmake') }}
      - name: Code Format Check
        run: |
          ./tools/format.sh check
      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - name: Test Git clone
        run: |
          git clone https://github.com/asmjit/asmjit.git
      - name: Install llvm
        run: |
          echo "current home is $HOME"
          export CUR_PROJECT=$(pwd)
          cd /opt
          # ./install_llvm15.sh
          # ./install_rust.sh
          cd $CUR_PROJECT
          export LLVM_SYS_150_PREFIX=/opt/llvm15
          export LLVM_DIR=$LLVM_SYS_150_PREFIX/lib/cmake/llvm
          export PATH=$LLVM_SYS_150_PREFIX/bin:$PATH
          cd tests/wast/spec
          git apply ../spec.patch
          cd $CUR_PROJECT
          export CMAKE_BUILD_TARGET=Debug
          export ENABLE_ASAN=true
          export RUN_MODE=multipass
          export INPUT_FORMAT=wasm
          export ENABLE_LAZY=true
          export ENABLE_MULTITHREAD=true
          export TestSuite=microsuite
          export CPU_EXCEPTION_TYPE='check'
          export ENABLE_GAS_METER=false

          bash .ci/run_test_suite.sh

  build_test_evmabi_mock_cli_on_x86:
    name: Build and test DTVM cli with evm abi hostapis on x86-64
    runs-on: ubuntu-latest
    container:
      image: dtvmdev1/dtvm-dev-x64:main
    steps:
      - name: Check out code
        uses: actions/checkout@v4
        with:
          submodules: "true"
      - name: Cache FetchContent deps
        uses: actions/cache@v4
        with:
          path: /github/home/.fetchcontent
          key: ${{ runner.os }}-fc-${{ github.workflow }}-ninja-v2-${{ hashFiles('third_party/AddDeps.cmake') }}
      - name: Code Format Check
        run: |
          ./tools/format.sh check
      - name: Setup sccache
        uses: mozilla-actions/sccache-action@v0.0.10
      - name: Test Git clone
        run: |
          git clone https://github.com/asmjit/asmjit.git
      - name: Install llvm
        run: |
          echo "current home is $HOME"
          export CUR_PROJECT=$(pwd)
          cd /opt
          # ./install_llvm15.sh
          # ./install_rust.sh
          cd $CUR_PROJECT
          export LLVM_SYS_150_PREFIX=/opt/llvm15
          export LLVM_DIR=$LLVM_SYS_150_PREFIX/lib/cmake/llvm
          export PATH=$LLVM_SYS_150_PREFIX/bin:$PATH
          cd tests/wast/spec
          git apply ../spec.patch
          cd $CUR_PROJECT

          DTVM_CI_BUILD_JOBS=7 .ci/cmake_ci_build.sh build -- \
            -DCMAKE_BUILD_TYPE=Debug \
            -DZEN_ENABLE_DWASM=ON \
            -DZEN_ENABLE_SINGLEPASS_JIT=ON \
            -DZEN_ENABLE_MULTIPASS_JIT=ON \
            -DZEN_ENABLE_CHECKED_ARITHMETIC=ON \
            -DZEN_ENABLE_CPU_EXCEPTION=OFF \
            -DZEN_ENABLE_EVMABI_TEST=ON \
            -DZEN_ENABLE_BUILTIN_LIBC=OFF \
            -DZEN_ENABLE_BUILTIN_ENV=OFF
          python3 .ci/print_sccache_stats.py || true
          # use dtvm to test evm abi wasm files
          # ./build/dtvm -m 2 -f call counter.wasm
